<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <artifactId>parent</artifactId>
    <groupId>fr.insee.rmes</groupId>
    <version>1.0.0</version>
  </parent>

  <artifactId>module-bauhaus-bo</artifactId>
  <version>4.21.0-rc8</version>
  <name>module-bauhaus-bo</name>

  <dependencies>
    <dependency>
      <groupId>fr.insee.rmes</groupId>
      <artifactId>module-utility</artifactId>
      <version>1.0.0</version>
    </dependency>
    <dependency>
      <groupId>org.jspecify</groupId>
      <artifactId>jspecify</artifactId>
      <version>1.0.0</version>
    </dependency>
    <dependency>
      <groupId>io.github.fbibonne</groupId>
      <artifactId>boot-properties-logger-starter</artifactId>
    </dependency>
    <!-- Swagger UI / OpenAPI, désactivé par défaut : voir OpenApiConfiguration et la propriété
         fr.insee.rmes.bauhaus.swagger.enabled. -->
    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
    </dependency>
    <dependency>
      <groupId>fr.insee.rmes</groupId>
      <artifactId>module-testcontainer-graphdb</artifactId>
      <version>1.0.0</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>fr.insee.rmes</groupId>
      <artifactId>module-ddi</artifactId>
      <version>1.0.0</version>
    </dependency>
    <!-- Cache des code lists mutualisées : abstraction Spring Cache + provider Caffeine
         (in-process, supporte expireAfterWrite/TTL). Voir ColecticaCacheConfiguration. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-cache</artifactId>
    </dependency>
    <dependency>
      <groupId>com.github.ben-manes.caffeine</groupId>
      <artifactId>caffeine</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-webmvc-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-resttestclient</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Fournit org.springframework.boot.restclient.RestTemplateBuilder, requis par
         TestRestTemplateTestAutoConfiguration de spring-boot-resttestclient (Spring Boot 4
         a séparé RestTemplateBuilder dans ce module et ne l'apporte pas transitivement). -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-restclient</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Enregistre SPARQLParserFactory dans le QueryParserRegistry : permet aux tests de rendre une
         requête et de la faire analyser par le parseur, sans dépôt. Le runtime n'en a pas besoin,
         c'est GraphDB qui analyse les requêtes envoyées par rdf4j-repository-http. -->
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-queryparser-sparql</artifactId>
      <version>${rdf4j.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <build>
    <sourceDirectory>src/main/java</sourceDirectory>
    <plugins>
    <!-- Exposes a property per dependency (e.g. ${org.mockito:mockito-core:jar}) used to load Mockito as a -javaagent -->
    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-dependency-plugin</artifactId>
      <executions>
        <execution>
          <id>get-mockito-agent-path</id>
          <goals>
            <goal>properties</goal>
          </goals>
        </execution>
      </executions>
    </plugin>
    <plugin>
      <groupId>org.apache.maven.plugins</groupId>
      <artifactId>maven-surefire-plugin</artifactId>
      <configuration>
        <useModulePath>false</useModulePath>
        <!-- @{argLine} keeps the JaCoCo agent; -javaagent loads Mockito explicitly so it
             no longer self-attaches (deprecated dynamic agent loading on JDK 21+) -->
        <argLine>@{argLine} -javaagent:${org.mockito:mockito-core:jar} -Xshare:off</argLine>
      </configuration>
    </plugin>
    </plugins>
  </build>
</project>
