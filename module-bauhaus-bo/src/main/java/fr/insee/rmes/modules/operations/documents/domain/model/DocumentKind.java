package fr.insee.rmes.modules.operations.documents.domain.model;

/** Un document (fichier téléversé) et un lien (page web) ont chacun leur propre espace d'identifiants. */
public enum DocumentKind {
    DOCUMENT,
    LINK
}
