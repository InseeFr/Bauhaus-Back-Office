<?xml version="1.0" encoding="UTF-8"?><project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
    <relativePath/>
    <!-- lookup parent from repository -->
  </parent>
  <groupId>fr.insee.rmes</groupId>
  <artifactId>parent</artifactId>
  <packaging>pom</packaging>
  <version>1.0.0</version>
  <name>RMES</name>
  <description>Back-office services for Bauhaus</description>
  <url>https://github.com/InseeFr/Bauhaus-Back-Office</url>
  <licenses>
    <license>
      <name>MIT License</name>
      <url>http://www.opensource.org/licences/MIT</url>
    </license>
  </licenses>
  <organization>
    <name>INSEE</name>
    <url>http://www.insee.fr</url>
  </organization>
  <developers>
    <developer>
      <name>Jérôme Bourgeois</name>
      <email>jerome.bourgeois@insee.fr</email>
    </developer>
  </developers>
  <properties>
    <java.version>25</java.version>
    <!-- springdoc 3.0.x est la ligne alignée sur Spring Boot 4.0.x (la 2.8.x ciblait Boot 3). -->
    <springdoc.version>3.0.3</springdoc.version>
    <rdf4j.version>5.3.1</rdf4j.version>
    <xdocreport.version>2.2.0</xdocreport.version>
    <jacoco.version>0.8.15</jacoco.version>
    <spotless.version>3.10.2</spotless.version>
    <palantir-java-format.version>2.98.0</palantir-java-format.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <!-- Default empty value so surefire's @{argLine} resolves even when JaCoCo is skipped -->
    <argLine></argLine>
    <java-jwt.version>4.5.2</java-jwt.version>
    <json.version>20260522</json.version>
    <flexmark.version>0.64.8</flexmark.version>
    <jaxb.version>4.0.9</jaxb.version>
    <zt.version>1.18.2</zt.version>
    <saxon.version>12.5</saxon.version>
    <minio.version>9.0.3</minio.version>
    <!-- okhttp 5.x est publié en Kotlin Multiplatform : l'artefact `okhttp` est un stub vide côté Maven,
         seul `okhttp-jvm` contient les classes. minio ne déclare que `okhttp`, d'où l'ajout explicite.
         Garder cette version alignée sur celle que minio déclare. -->
    <okhttp.version>5.3.2</okhttp.version>
    <jetbrains-annotations.version>24.0.1</jetbrains-annotations.version>
    <record.builder.version>53</record.builder.version>
    <!-- SONAR -->
    <sonar.organization>inseefr</sonar.organization>
    <sonar.host.url>https://sonarcloud.io</sonar.host.url>
    <sonar.projectKey>InseeFr_Bauhaus-Back-Office</sonar.projectKey>
    <boot-properties-logger-starter.version>2.2.0</boot-properties-logger-starter.version>
    <ddi-lifecycle.version>2.0.0</ddi-lifecycle.version>
    <testcontainers-keycloak.version>4.2.1</testcontainers-keycloak.version>
    <testcontainers.version>1.21.4</testcontainers.version>
    <!-- Overrides Spring Boot 4.0.7 (5.3.6), vulnerable to CVE-2026-54399 (httpcore5, HTTP/1.1 parser)
         and CVE-2026-54428 (httpcore5-h2, HPACK decoder). Both fixed in 5.4.3.
         À supprimer dès qu'une version de Spring Boot managera >= 5.4.3. -->
    <httpcore5.version>5.4.3</httpcore5.version>
    <!-- Surcharge le Tomcat embarqué managé par Spring Boot 4.1.0 (11.0.22), affecté entre autres par
         CVE-2026-68763 (fuite d'allocation dans le suivi du backlog HTTP/2 lors d'un reset de stream, DoS)
         et CVE-2026-68569 (authentification qui réussit pour un utilisateur absent du realm), toutes deux
         corrigées en 11.0.25.
         À supprimer dès qu'une version de Spring Boot managera >= 11.0.25. -->
    <tomcat.version>11.0.25</tomcat.version>
    <!-- Docker API version forced for Testcontainers (workaround: it falls back to 1.32 which is rejected by Docker Engine 20.10+ that requires min API 1.40) -->
    <docker.api.version>1.41</docker.api.version>
    <!-- Voir la dépendance bcprov-jdk18on ci-dessous : surcharge de la version que minio ramène. -->
    <bouncycastle.version>1.85.2</bouncycastle.version>
  </properties>

  <dependencyManagement>
    <dependencies>
    <!-- minio 9.0.3 ramène bcprov 1.84, vulnérable : on force 1.85.2 (#1140).
         À supprimer dès que minio embarquera >= 1.85.2 — la condition d'origine
         (« quand on passera à minio 8.6.0 ») est caduque, la branche est en 9.0.3. -->
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcprov-jdk18on</artifactId>
      <version>${bouncycastle.version}</version>
    </dependency>
      <dependency>
        <groupId>io.github.fbibonne</groupId>
        <artifactId>boot-properties-logger-starter</artifactId>
        <version>${boot-properties-logger-starter.version}</version>
      </dependency>
      <dependency>
        <groupId>fr.insee.ddi</groupId>
        <artifactId>ddi-lifecycle</artifactId>
        <version>${ddi-lifecycle.version}</version>
      </dependency>
      <dependency>
        <groupId>org.springdoc</groupId>
        <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        <version>${springdoc.version}</version>
      </dependency>
      <dependency>
        <groupId>com.github.dasniko</groupId>
        <artifactId>testcontainers-keycloak</artifactId>
        <version>${testcontainers-keycloak.version}</version>
      </dependency>
      <!-- kotlin-stdlib (via okhttp-jvm, déclaré directement) tire annotations 13.0, dont @NotNull n'a pas
           de target TYPE_USE : sans ce pin il gagnerait la médiation face au 24.0.1 de flexmark (plus profond)
           et casserait la compilation des `@NotNull Path` de XsltUtilsTest. -->
      <dependency>
        <groupId>org.jetbrains</groupId>
        <artifactId>annotations</artifactId>
        <version>${jetbrains-annotations.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-jackson2</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-hateoas</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.data</groupId>
      <artifactId>spring-data-rest-hal-explorer</artifactId>
    </dependency>
    <dependency>
      <groupId>io.minio</groupId>
      <artifactId>minio</artifactId>
      <version>${minio.version}</version>
    </dependency>
    <dependency>
      <groupId>com.squareup.okhttp3</groupId>
      <artifactId>okhttp-jvm</artifactId>
      <version>${okhttp.version}</version>
    </dependency>
    <dependency>
      <groupId>com.fasterxml.jackson.dataformat</groupId>
      <artifactId>jackson-dataformat-xml</artifactId>
    </dependency>
    <dependency>
      <groupId>org.zeroturnaround</groupId>
      <artifactId>zt-zip</artifactId>
      <version>${zt.version}</version>
      <type>jar</type>
    </dependency>
    <dependency>
      <groupId>jakarta.xml.bind</groupId>
      <artifactId>jakarta.xml.bind-api</artifactId>
    </dependency>
    <dependency>
      <groupId>org.glassfish.jaxb</groupId>
      <artifactId>jaxb-runtime</artifactId>
      <version>${jaxb.version}</version>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-oxm</artifactId>
    </dependency>
    <dependency>
      <groupId>jakarta.validation</groupId>
      <artifactId>jakarta.validation-api</artifactId>
    </dependency>
    <!-- L'API seule ne valide rien : le starter apporte hibernate-validator et le
         MethodValidationPostProcessor qui déclenche @Valid sur les @RequestBody. -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <!-- SECURITY -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>com.auth0</groupId>
      <artifactId>java-jwt</artifactId>
      <version>${java-jwt.version}</version>
    </dependency>
    <!-- test -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <exclusions>
        <exclusion>
          <groupId>com.vaadin.external.google</groupId>
          <artifactId>android-json</artifactId>
        </exclusion>
      </exclusions>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.xmlunit</groupId>
      <artifactId>xmlunit-core</artifactId>
      <scope>test</scope>
    </dependency>

    <!-- RDF -->
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-repository-http</artifactId>
      <version>${rdf4j.version}</version>
      <exclusions>
        <exclusion>
          <groupId>org.slf4j</groupId>
          <artifactId>jcl-over-slf4j</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-queryresultio-sparqljson</artifactId>
      <version>${rdf4j.version}</version>
    </dependency>
    <!-- to load trig files in database-->
    <dependency>
      <groupId>org.eclipse.rdf4j</groupId>
      <artifactId>rdf4j-rio-trig</artifactId>
      <version>${rdf4j.version}</version>
    </dependency>
    <!-- Manage JSONObject and JSONArray -->
    <dependency>
      <groupId>org.json</groupId>
      <artifactId>json</artifactId>
      <version>${json.version}</version>
    </dependency>
    <!-- SAXON FOR XSLT -->
    <dependency>
      <groupId>net.sf.saxon</groupId>
      <artifactId>Saxon-HE</artifactId>
      <version>${saxon.version}</version>
      <exclusions>
        <exclusion>
          <groupId>xml-apis</groupId>
          <artifactId>xml-apis</artifactId>
        </exclusion>
      </exclusions>
    </dependency>
    <!-- FREEMARKER (for queries) -->
    <dependency>
      <groupId>org.freemarker</groupId>
      <artifactId>freemarker</artifactId>
    </dependency>
    <dependency>
      <groupId>fr.opensagres.xdocreport</groupId>
      <artifactId>fr.opensagres.xdocreport.document.odt</artifactId>
      <version>${xdocreport.version}</version>
    </dependency>
    <dependency>
      <groupId>fr.opensagres.xdocreport</groupId>
      <artifactId>fr.opensagres.xdocreport.template.freemarker</artifactId>
      <version>${xdocreport.version}</version>
    </dependency>
    <!-- XHTML to Markdown parser -->
    <dependency>
      <groupId>com.vladsch.flexmark</groupId>
      <artifactId>flexmark-html2md-converter</artifactId>
      <version>${flexmark.version}</version>
    </dependency>
    <dependency>
      <groupId>io.soabase.record-builder</groupId>
      <artifactId>record-builder-core</artifactId>
      <version>${record.builder.version}</version>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>minio</artifactId>
      <version>${testcontainers.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <version>1.4.1</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <version>${testcontainers.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>
  <build>
    <plugins>

      <!-- Formatage du code Java. `spotless:apply` reformate, `spotless:check` vérifie ;
           le check est lié à `validate`, donc tout build (y compris la CI, qui lance
           `verify`) échoue sur un fichier non formaté, avant même la compilation.
           Pour l'ignorer ponctuellement : -Dspotless.check.skip=true -->
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless.version}</version>
        <configuration>
          <java>
            <!-- Formateur figé : pas d'option de style, comme Prettier côté front.
                 Palantir impose 4 espaces et 120 colonnes, ce que le code suivait déjà. -->
            <palantirJavaFormat>
              <version>${palantir-java-format.version}</version>
            </palantirJavaFormat>
          </java>
        </configuration>
        <executions>
          <execution>
            <id>spotless-check</id>
            <phase>validate</phase>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
<!--           <compilerArgs>-->
<!--               <arg>-XDcompilePolicy=simple</arg>-->
<!--               <arg>&#45;&#45;should-stop=ifError=FLOW</arg>-->
<!--               <arg>-Xplugin:ErrorProne -Xep:NullAway:ERROR -XepOpt:NullAway:OnlyNullMarked</arg>-->
<!--               <arg>-J&#45;&#45;add-exports=jdk.compiler/com.sun.tools.javac.api=ALL-UNNAMED</arg>-->
<!--               <arg>-J&#45;&#45;add-exports=jdk.compiler/com.sun.tools.javac.file=ALL-UNNAMED</arg>-->
<!--               <arg>-J&#45;&#45;add-exports=jdk.compiler/com.sun.tools.javac.main=ALL-UNNAMED</arg>-->
<!--               <arg>-J&#45;&#45;add-exports=jdk.compiler/com.sun.tools.javac.model=ALL-UNNAMED</arg>-->
<!--               <arg>-J&#45;&#45;add-exports=jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED</arg>-->
<!--               <arg>-J&#45;&#45;add-exports=jdk.compiler/com.sun.tools.javac.processing=ALL-UNNAMED</arg>-->
<!--               <arg>-J&#45;&#45;add-exports=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED</arg>-->
<!--               <arg>-J&#45;&#45;add-exports=jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED</arg>-->
<!--               <arg>-J&#45;&#45;add-opens=jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED</arg>-->
<!--               <arg>-J&#45;&#45;add-opens=jdk.compiler/com.sun.tools.javac.comp=ALL-UNNAMED</arg>-->
<!--           </compilerArgs>-->
           <annotationProcessorPaths>
            <path>
              <groupId>io.soabase.record-builder</groupId>
              <artifactId>record-builder-processor</artifactId>
              <version>${record.builder.version}</version>
            </path>
<!--               <path>-->
<!--                   <groupId>com.google.errorprone</groupId>-->
<!--                   <artifactId>error_prone_core</artifactId>-->
<!--                   <version>2.43.0</version>-->
<!--               </path>-->
<!--               <path>-->
<!--                   <groupId>com.uber.nullaway</groupId>-->
<!--                   <artifactId>nullaway</artifactId>-->
<!--                   <version>0.12.12</version>-->
<!--               </path>-->
          </annotationProcessorPaths>
        </configuration>
        <executions>
          <execution>
            <id>default-testCompile</id>
            <configuration>
              <proc>none</proc>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
      <!-- Forces Docker Engine API version for Testcontainers (docker-java defaults to 1.32 which Docker Engine 20.10+ rejects). -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <systemPropertyVariables>
            <api.version>${docker.api.version}</api.version>
          </systemPropertyVariables>
        </configuration>
      </plugin>
      <!-- Plugin tests -->
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <executions>
      <execution>
            <id>pre-unit-test</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>post-unit-test</id>
            <phase>test</phase>
            <goals>
              <goal>report</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
  <repositories>
    <repository>
      <id>maven_central</id>
      <name>Maven Central</name>
      <url>https://repo.maven.apache.org/maven2/</url>
    </repository>
  </repositories>
  <modules>
    <module>colectica-client</module>
    <module>module-utility</module>
    <module>module-testcontainer-graphdb</module>
    <module>module-operation</module>
    <module>module-ddi</module>
    <module>module-bauhaus-bo</module>
  </modules>
</project>
